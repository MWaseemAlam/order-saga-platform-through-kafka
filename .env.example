# These match the defaults docker-compose already sets for each service -
# only needed if you're running a service outside Docker against the
# compose-managed Kafka/Postgres.
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/order_db
